<h1><%=params[:type].eql?('accessor') ? 'Listing All Accessors' : 'Listing All Students'%></h1>
<% if @users.blank? %>
    Sorry No Users Found....
<% else %>
    <ul>
      <% for user in @users %>
      <li><%= user.email %></li>
       <% end %>
    </ul>
<% end %>
